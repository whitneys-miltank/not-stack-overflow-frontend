var app = angular.module('not-stack-overflow', []);

app.service('questionService', function($http) {
	this.add = function(submission) {

	}
});

app.controller('controller', function($scope, questionService) {

});
